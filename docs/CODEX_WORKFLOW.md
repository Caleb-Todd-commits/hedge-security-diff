# Codex workflow and Build Week evidence

## Primary build session

Keep the majority of core implementation in one primary Codex session and preserve its `/feedback` session ID for Devpost. Use that session for architecture, AST extraction, graph delta, GitHub integration, model schemas, tests, evaluation, remediation, and demo reliability—not merely polishing.

## Repository contract

`AGENTS.md` defines the standing constraints Codex must follow. Significant decisions belong in `docs/DECISIONS.md`, including whether the human or model made the call and what evidence changed it.

## Product remediation flow

The example `@hedge fix HEDGE-NNN` workflow:

1. Accepts only one strict command.
2. Confirms the commenter has repository write permission.
3. Rejects fork PRs.
4. Reads the latest machine-readable Hedge report generated by GitHub Actions.
5. Delimits the finding as untrusted evidence beneath a trusted repair contract.
6. Runs `openai/codex-action@v1` with workspace-write sandboxing and no push credentials.
7. Captures a digest-bound text patch and bounded, mention-safe Codex summary.
8. Transfers the artifact to a secretless read-only job that rejects unsafe paths, modes, binaries, excessive size, and stale source bindings before running target validation without network access.
9. Revalidates the current PR head in a separate write-authorized job and opens at most one draft PR against the original PR branch.
10. Requires immutable-witness counterfactual verification before closure.

## Live proof status

The judge-lab repository now records the original security diff, benign silence with zero model calls, an authorized fix command, and successful source-bound Codex patch production. Automated remediation validation stopped on the fixture's pre-existing empty Vitest suite, so the exact patch was recovered as an explicitly experimental draft without another model call.

Counterfactual verification run `29705400423` passed vulnerable witness reproduction, repaired witness blocking, legitimate behavior, and an evidence-linked architecture control change. Reviewable state PR 8 recorded HEDGE-009 as `verified` on protected `main`. Treat this as one proven canary, not a guarantee that arbitrary witnesses or repairs will pass.
