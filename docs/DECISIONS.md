# Decision log

| ID    | Decision                                                                                            | Owner                                        | Why                                                                                          | Revisit trigger                                                      |
| ----- | --------------------------------------------------------------------------------------------------- | -------------------------------------------- | -------------------------------------------------------------------------------------------- | -------------------------------------------------------------------- |
| D-001 | Make “security architecture diff” the primary product, not generic living threat-model generation.  | Human                                        | Existing products already generate living threat models; the delta is the sharper primitive. | A competitor already owns evidence-linked security diffs at PR time. |
| D-002 | Support TypeScript and Next.js App Router first.                                                    | Human + Codex implementation                 | A narrow reliable extractor is more credible than broad regex support.                       | Next.js demo is stable and all required tests pass.                  |
| D-003 | Establish architecture facts deterministically before model interpretation.                         | Human                                        | Prevents the model from inventing the system it is reviewing.                                | None for Build Week.                                                 |
| D-004 | Use Luna only after a deterministic delta, then Sol for deep interpretation.                        | Human                                        | Reduces cost and noise while preserving strong reasoning where it matters.                   | Measured routing recall is poor.                                     |
| D-005 | Stay silent when no graph delta exists.                                                             | Human                                        | Noise destroys security-tool adoption.                                                       | False-negative evaluation shows the graph abstraction is too narrow. |
| D-006 | Keep CWE optional.                                                                                  | Human                                        | Architectural risks do not always map cleanly to a weakness ID.                              | None.                                                                |
| D-007 | Require counterfactual evidence before `verified`.                                                  | Human                                        | A test file or passing test alone is not proof of mitigation.                                | None.                                                                |
| D-008 | Treat prompt injection primarily as an isolation problem, not automatically an application finding. | Human                                        | Test fixtures and security research legitimately contain instruction-like text.              | Evidence shows repository content altered analysis behavior.         |
| D-009 | Restrict hackathon workflows to same-repository PRs.                                                | Human                                        | Fork-secret handling is easy to implement unsafely and not required for the demo.            | Production hardening phase.                                          |
| D-010 | `@hedge fix` opens a draft PR and never pushes directly to the protected branch.                    | Human                                        | Preserves review and limits blast radius.                                                    | None.                                                                |
| D-011 | Use Node 24 action metadata.                                                                        | Codex-assisted, verified against GitHub docs | GitHub supports Node 24 for JavaScript actions and Node 20 is being deprecated.              | GitHub runtime guidance changes.                                     |
| D-012 | Do not claim a typical run cost until it is measured.                                               | Human                                        | Pricing plus token volumes must be observed, not guessed.                                    | Evaluation dataset is complete.                                      |

Add entries whenever product behavior, safety, scope, or evaluation changes. The final submission should state where Codex proposed implementation details and where the human made product and security decisions.

## Additional implemented decisions

| ID    | Decision                                                                                                    | Owner                        | Why                                                                                                               | Revisit trigger                                                             |
| ----- | ----------------------------------------------------------------------------------------------------------- | ---------------------------- | ----------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- |
| D-013 | Load PR policy, context, and register only from the trusted base SHA.                                       | Human + Codex implementation | A PR must not weaken the tool reviewing that same PR.                                                             | Never for PR analysis.                                                      |
| D-014 | Treat a missing base register as an explicit empty trusted baseline.                                        | Human + Codex implementation | Falling back to PR-head `threatmodel.json` would permit state poisoning.                                          | Never.                                                                      |
| D-015 | Resolve every model evidence reference against deterministic graph evidence and omit unsupported proposals. | Human                        | Structured syntax alone does not ground a security claim.                                                         | A stronger signed-evidence protocol replaces it.                            |
| D-016 | Add five reviewed context fields for facts code cannot safely infer.                                        | Human                        | Sensitivity, exposure, roles, and trust relationships are often deployment facts.                                 | User research shows a smaller or better set.                                |
| D-017 | Split Codex remediation and GitHub publishing into separate jobs connected by a patch artifact.             | Human                        | The job with the model credential should not also possess repository-write authority.                             | A more secure first-party broker replaces the split.                        |
| D-018 | Make verification evidence portable JSON and require architecture confirmation before `verified`.           | Human                        | Executable witnesses prove behavior; the graph proves the intended control changed.                               | None.                                                                       |
| D-019 | Apply middleware only when framework path/order semantics support it.                                       | Human + Codex implementation | Treating nearby middleware as protection creates dangerous false negatives.                                       | Additional framework semantics are implemented.                             |
| D-020 | Model exported Next.js Server Actions as callable entry points.                                             | Human + Codex implementation | They cross a client/server boundary even without an HTTP route file.                                              | Next.js changes the Server Action execution model.                          |
| D-021 | Keep graph identities semantic and independent of source line.                                              | Human + Codex implementation | Comment insertion and formatting must not produce fake architecture deltas.                                       | A stronger language-server identity is available.                           |
| D-022 | Seal the complete register with atomic, versioned state integrity.                                          | Human + Codex implementation | Findings, verification, and acceptance history matter as much as the graph.                                       | External signing or transparency log is added.                              |
| D-023 | Force deep review for deterministically sensitive deltas.                                                   | Human                        | Cheap model triage must not veto review of secrets, privileged execution, public mutation, or workflow privilege. | Measured deep-review false positives require a narrower deterministic gate. |
| D-024 | Bind machine handoffs to the exact source commit and digest.                                                | Human + Codex implementation | A valid old report must not authorize remediation against changed code.                                           | A first-party signed command channel replaces comments.                     |
| D-025 | Treat model-generated report text as untrusted output.                                                      | Human + Codex implementation | Reports can trigger HTML, mention, Markdown, or diagram rendering side effects.                                   | None.                                                                       |

## v0.5 structural decisions

| ID    | Decision                                                                                                             | Owner                        | Why                                                                                                                               | Revisit trigger                                                          |
| ----- | -------------------------------------------------------------------------------------------------------------------- | ---------------------------- | --------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| D-026 | Make repository security invariants first-class configuration, separate from inferred findings.                      | Human + Codex implementation | Teams need explicit commitments that remain stable across model changes and can be evaluated deterministically.                   | Invariants require a richer temporal or cross-service language.          |
| D-027 | Persist deterministic observations, confidence-bearing inferences, and policy decisions as separate analysis layers. | Human                        | An LLM hypothesis must never silently become an architectural fact or merge verdict.                                              | None.                                                                    |
| D-028 | Make the recorded decision the GitHub Action failure authority.                                                      | Human + Codex implementation | Failure behavior should be explainable by threshold or invariant policy, not free-form text.                                      | A centralized policy service replaces local decisions.                   |
| D-029 | Add full base/head replay fixtures with optional recorded model boundaries.                                          | Human + Codex implementation | Demonstrations and regressions must reproduce the complete pipeline without pretending a recorded response is a fresh API result. | Live ephemeral integration environments become reliable and inexpensive. |
| D-030 | Require the committed npm lockfile to resolve only through the public npm registry.                                  | Codex implementation         | A lockfile bound to an internal package gateway cannot be installed by judges or public CI.                                       | A documented public package mirror is intentionally adopted.             |
| D-031 | Publish the recorded standalone dashboard as the no-install judge path.                                              | Human + Codex implementation | Developer-tool submissions require a test path without rebuilding; the artifact is useful if clearly labeled as recorded.         | A safe live sandbox replaces the recorded artifact.                      |
| D-032 | Omit runtime-generated timestamps from public JSON Schema defaults.                                                  | Codex implementation         | Embedding wall-clock defaults makes schema generation nondeterministic and misrepresents a value assigned only at runtime.        | JSON Schema gains an explicit dynamic-default vocabulary.                |
| D-033 | Commit the bundled Node 24 Action entry point while keeping the CLI as a release artifact.                           | Codex implementation         | GitHub Actions execute repository contents directly; an ignored Action bundle makes every source ref uninstallable.               | GitHub supports building JavaScript actions before execution.            |
