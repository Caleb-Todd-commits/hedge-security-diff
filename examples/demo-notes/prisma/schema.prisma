model User {
  id    String @id
  notes Note[]
}

model Note {
  id      String @id
  ownerId String
  owner   User   @relation(fields: [ownerId], references: [id])
  title   String
  body    String
}
